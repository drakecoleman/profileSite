const nodemailer = require("nodemailer");
// async..await is not allowed in global scope, must use a wrapper
// async function main() {
// create reusable transporter object using the default SMTP transport
// var transporter = nodemailer.createTransport({
//   host: "smtp.gmail.com", // hostname
//   auth: {
//     user: "moonstarcodemail@gmail.com",
//     pass: "Moon_2021!",
//   },
// });

// send mail with defined transport object
// let info = await transporter.sendMail({
//   from: '"drakecoleman@rocketmail.com', // sender address
//   to: "support@moonstar.biz", // list of receivers
//   subject: "Hello ✔", // Subject line
//   text: "Hello world?", // plain text body
//   html: "<b>Hello world?</b>", // html body
// });

// console.log("Message sent: %s", info.messageId);
// Message sent: <b658f8ca-6296-ccf4-8306-87d57a0b4321@example.com>
// }

// main().catch(console.error);





Your main line is Alpha and Zeta, use Alpha tributing Zeta to add Benten, then use Zeta tributing Benten and add Meteonis, use Benten to add Diviner and Normal Summon her sending Arc Light to add Idaten, then go into Fafnir sending Gamma. Next use Meteonis to Ritual the Benten back, then use Gamma tributing the Benten to Summon back the monster you used for Meteonis, use Benten to add Orange Light or Lancea if you have that in main/side (is a preference option). Now with this field, use Meteonistargeting Fafnir to add it back to hand, then Ritual Summon Idaten using the Meteonis you added back, then use Idaten to add back said Meteonis you just use, now you have 2 level 6s because Diviner modulates herself to go into Beatrice, use Beatrice to send Ultimateness and use your last Ritual Summon to get it on your field, use the remaining Fafnir and either Gamma or the monster you Summoned back off said Gamma to go into I:P. On your opponents turn, use Beatrice in DP or SP sending Eva to add more Herald fodder. (edited)
